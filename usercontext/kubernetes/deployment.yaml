apiVersion: apps/v1
kind: Deployment
metadata:
  name: usercontext
  labels:
    app: usercontext
spec:
  replicas: 1
  selector:
    matchLabels:
      app: usercontext
  template:
    metadata:
      labels:
        app: usercontext
    spec:
      containers:
        - name: usercontext
          image: "a071098registry.azurecr.io/usercontext:v1"
          ports:
            - containerPort: 8000
          # [추가] secret.yaml에 정의된 값들을 환경 변수로 주입합니다.
          envFrom:
            - secretRef:
                name: api-keys
            - configMapRef:
                name: my-configs
          readinessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 2
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 2
            periodSeconds: 10
            failureThreshold: 30